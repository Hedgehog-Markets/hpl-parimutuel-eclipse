/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import type { AccountTypeArgs } from "../types";
import type {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
} from "@metaplex-foundation/umi";
import type { Serializer } from "@metaplex-foundation/umi/serializers";

import {
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from "@metaplex-foundation/umi";
import {
  array,
  bool,
  mapSerializer,
  publicKey as publicKeySerializer,
  string,
  struct,
  u64,
  u8,
} from "@metaplex-foundation/umi/serializers";

import { AccountType, getAccountTypeSerializer } from "../types";

export type UserPositionV1 = Account<UserPositionV1AccountData>;

export type UserPositionV1AccountData = {
  accountType: AccountType;
  market: PublicKey;
  wallet: PublicKey;
  claimed: boolean;
  amounts: Array<bigint>;
};

export type UserPositionV1AccountDataArgs = {
  market: PublicKey;
  wallet: PublicKey;
  claimed: boolean;
  amounts: Array<number | bigint>;
};

export function getUserPositionV1AccountDataSerializer(): Serializer<
  UserPositionV1AccountDataArgs,
  UserPositionV1AccountData
> {
  return mapSerializer<UserPositionV1AccountDataArgs, any, UserPositionV1AccountData>(
    struct<UserPositionV1AccountData>(
      [
        ["accountType", getAccountTypeSerializer()],
        ["market", publicKeySerializer()],
        ["wallet", publicKeySerializer()],
        ["claimed", bool()],
        ["amounts", array(u64(), { size: u8() })],
      ],
      { description: "UserPositionV1AccountData" },
    ),
    (value) => ({ ...value, accountType: AccountType.UserPositionV1 }),
  );
}

export function deserializeUserPositionV1(rawAccount: RpcAccount): UserPositionV1 {
  return deserializeAccount(rawAccount, getUserPositionV1AccountDataSerializer());
}

export async function fetchUserPositionV1(
  context: Pick<Context, "rpc">,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions,
): Promise<UserPositionV1> {
  const maybeAccount = await context.rpc.getAccount(toPublicKey(publicKey, false), options);
  assertAccountExists(maybeAccount, "UserPositionV1");
  return deserializeUserPositionV1(maybeAccount);
}

export async function safeFetchUserPositionV1(
  context: Pick<Context, "rpc">,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions,
): Promise<UserPositionV1 | null> {
  const maybeAccount = await context.rpc.getAccount(toPublicKey(publicKey, false), options);
  return maybeAccount.exists ? deserializeUserPositionV1(maybeAccount) : null;
}

export async function fetchAllUserPositionV1(
  context: Pick<Context, "rpc">,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions,
): Promise<Array<UserPositionV1>> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options,
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, "UserPositionV1");
    return deserializeUserPositionV1(maybeAccount);
  });
}

export async function safeFetchAllUserPositionV1(
  context: Pick<Context, "rpc">,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions,
): Promise<Array<UserPositionV1>> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options,
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) => deserializeUserPositionV1(maybeAccount as RpcAccount));
}

export function getUserPositionV1GpaBuilder(context: Pick<Context, "rpc" | "programs">) {
  const programId = context.programs.getPublicKey(
    "hplParimutuel",
    "PARrVs6F5egaNuz8g6pKJyU4ze3eX5xGZCFb3GLiVvu",
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      accountType: AccountTypeArgs;
      market: PublicKey;
      wallet: PublicKey;
      claimed: boolean;
      amounts: Array<number | bigint>;
    }>({
      accountType: [0, getAccountTypeSerializer()],
      market: [1, publicKeySerializer()],
      wallet: [33, publicKeySerializer()],
      claimed: [65, bool()],
      amounts: [66, array(u64(), { size: u8() })],
    })
    .deserializeUsing<UserPositionV1>((account) => deserializeUserPositionV1(account))
    .whereField("accountType", AccountType.UserPositionV1);
}

export function findUserPositionV1Pda(
  context: Pick<Context, "eddsa" | "programs">,
  seeds: {
    /** The address of the market. */
    market: PublicKey;
    /** The address of the user wallet. */
    wallet: PublicKey;
  },
): Pda {
  const programId = context.programs.getPublicKey(
    "hplParimutuel",
    "PARrVs6F5egaNuz8g6pKJyU4ze3eX5xGZCFb3GLiVvu",
  );
  return context.eddsa.findPda(programId, [
    string({ size: "variable" }).serialize("user_position"),
    publicKeySerializer().serialize(seeds.market),
    publicKeySerializer().serialize(seeds.wallet),
  ]);
}

export async function fetchUserPositionV1FromSeeds(
  context: Pick<Context, "eddsa" | "programs" | "rpc">,
  seeds: Parameters<typeof findUserPositionV1Pda>[1],
  options?: RpcGetAccountOptions,
): Promise<UserPositionV1> {
  return fetchUserPositionV1(context, findUserPositionV1Pda(context, seeds), options);
}

export async function safeFetchUserPositionV1FromSeeds(
  context: Pick<Context, "eddsa" | "programs" | "rpc">,
  seeds: Parameters<typeof findUserPositionV1Pda>[1],
  options?: RpcGetAccountOptions,
): Promise<UserPositionV1 | null> {
  return safeFetchUserPositionV1(context, findUserPositionV1Pda(context, seeds), options);
}
