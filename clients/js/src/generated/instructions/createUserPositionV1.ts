/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import type { ResolvedAccount, ResolvedAccountsWithIndices } from "../shared";
import type { Context, Pda, PublicKey, Signer, TransactionBuilder } from "@metaplex-foundation/umi";
import type { Serializer } from "@metaplex-foundation/umi/serializers";

import { transactionBuilder } from "@metaplex-foundation/umi";
import { mapSerializer, struct, u8 } from "@metaplex-foundation/umi/serializers";

import { findUserPositionV1Pda, findUserV1Pda } from "../accounts";
import { expectPublicKey, getAccountMetasAndSigners } from "../shared";

// Accounts.
export type CreateUserPositionV1InstructionAccounts = {
  /** Market */
  market: PublicKey | Pda;
  /** User position */
  userPosition?: PublicKey | Pda;
  /** User */
  user?: PublicKey | Pda;
  /** User wallet */
  wallet?: Signer;
  /** Payer */
  payer?: Signer;
  /** System program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type CreateUserPositionV1InstructionData = { discriminator: number };

export type CreateUserPositionV1InstructionDataArgs = {};

export function getCreateUserPositionV1InstructionDataSerializer(): Serializer<
  CreateUserPositionV1InstructionDataArgs,
  CreateUserPositionV1InstructionData
> {
  return mapSerializer<
    CreateUserPositionV1InstructionDataArgs,
    any,
    CreateUserPositionV1InstructionData
  >(
    struct<CreateUserPositionV1InstructionData>([["discriminator", u8()]], {
      description: "CreateUserPositionV1InstructionData",
    }),
    (value) => ({ ...value, discriminator: 3 }),
  );
}

// Instruction.
export function createUserPositionV1(
  context: Pick<Context, "eddsa" | "identity" | "payer" | "programs">,
  input: CreateUserPositionV1InstructionAccounts,
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    "hplParimutuel",
    "PARrVs6F5egaNuz8g6pKJyU4ze3eX5xGZCFb3GLiVvu",
  );

  // Accounts.
  const resolvedAccounts = {
    market: {
      index: 0,
      isWritable: false as boolean,
      value: input.market ?? null,
    },
    userPosition: {
      index: 1,
      isWritable: true as boolean,
      value: input.userPosition ?? null,
    },
    user: { index: 2, isWritable: false as boolean, value: input.user ?? null },
    wallet: {
      index: 3,
      isWritable: false as boolean,
      value: input.wallet ?? null,
    },
    payer: {
      index: 4,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    systemProgram: {
      index: 5,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.wallet.value) {
    resolvedAccounts.wallet.value = context.identity;
  }
  if (!resolvedAccounts.userPosition.value) {
    resolvedAccounts.userPosition.value = findUserPositionV1Pda(context, {
      market: expectPublicKey(resolvedAccounts.market.value),
      wallet: expectPublicKey(resolvedAccounts.wallet.value),
    });
  }
  if (!resolvedAccounts.user.value) {
    resolvedAccounts.user.value = findUserV1Pda(context, {
      wallet: expectPublicKey(resolvedAccounts.wallet.value),
    });
  }
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111",
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: Array<ResolvedAccount> = Object.values(resolvedAccounts).sort(
    (a, b) => a.index - b.index,
  );

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(orderedAccounts, "programId", programId);

  // Data.
  const data = getCreateUserPositionV1InstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
